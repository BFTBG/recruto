services:
  - type: web
    name: recruto-hello
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    repo: https://github.com/BFTBG/recruto.git